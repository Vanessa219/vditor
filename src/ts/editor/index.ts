export class Editor {
    readonly id: string
    readonly option: Options

    constructor(id: string, option:Options) {
        this.id = id
        this.option = option
    }

    genHTML(){

    }
}